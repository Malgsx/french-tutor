# Copy to .env next to package.json, then: chmod 600 .env
# Never commit .env. Never put a key in VITE_*, renderer code, or a chat.

# Optional. Demo and avatar customization work without these.
# LIVE_ENABLED=true
# OPENAI_API_KEY=
